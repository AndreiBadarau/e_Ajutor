<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/request_card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="8dp"
    app:cardElevation="4dp"
    app:cardCornerRadius="8dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Status Indicator (Small Vertical Bar) -->
        <View
            android:id="@+id/view_status_indicator_item"
            android:layout_width="6dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            tools:background="@color/status_indicator_yellow" /> <!-- Default/tool color -->

        <!-- Benefit Icon (Optional) -->
        <ImageView
            android:id="@+id/imageView_benefit_icon_item"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="12dp"
            app:layout_constraintStart_toEndOf="@id/view_status_indicator_item"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:srcCompat="@drawable/ic_benefit_placeholder"
            android:contentDescription="@string/benefit_type_icon" />

        <!-- Benefit Type -->
        <TextView
            android:id="@+id/textView_benefit_type_item"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp"
            android:textAppearance="?attr/textAppearanceListItem"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@id/imageView_benefit_icon_item"
            app:layout_constraintTop_toTopOf="@id/imageView_benefit_icon_item"
            app:layout_constraintEnd_toStartOf="@+id/textView_status_item"
            tools:text="Unemployment Benefit Application" />

        <!-- Submission Date -->
        <TextView
            android:id="@+id/textView_date_item"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textAppearance="?attr/textAppearanceCaption"
            android:textSize="12sp"
            app:layout_constraintStart_toStartOf="@id/textView_benefit_type_item"
            app:layout_constraintTop_toBottomOf="@id/textView_benefit_type_item"
            app:layout_constraintEnd_toEndOf="@id/textView_benefit_type_item"
            tools:text="Oct 26, 2023" />

        <!-- Status Text (Aligned to the end) -->
        <TextView
            android:id="@+id/textView_status_item"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceBody2"
            android:textStyle="italic"
            android:textSize="14sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/textView_benefit_type_item"
            app:layout_constraintBottom_toBottomOf="@id/textView_benefit_type_item"
            tools:text="In curs"
            tools:textColor="@color/status_indicator_yellow"/>

        <!-- User Identifier (For Operator View - initially GONE) -->
        <TextView
            android:id="@+id/textView_user_identifier_item"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textAppearance="?attr/textAppearanceOverline"
            android:textSize="11sp"
            android:visibility="gone"
            app:layout_constraintStart_toStartOf="@id/textView_benefit_type_item"
            app:layout_constraintTop_toBottomOf="@id/textView_date_item"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:text="User: example@user.com"
            tools:visibility="visible"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>